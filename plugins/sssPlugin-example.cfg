## where is the synology server running and credentials to connect to it.
## copy this file to sssPlugin.cfg and set the values appropriately
[sss]
server = <your synology Server address>
port = 5000
user = <user name>
version = <synology Web API version>
password = <password>
